const reversestring = require('./reversestring');

describe('test function reversestringSolution1', () => {
  test('given apple should return elppa', () => {
    expect(reversestring.reversestringSolution1('apple')).toBe('elppa');
  });
});

describe('test function reversestringSolution2', () => {
  test('given apple should return elppa', () => {
    expect(reversestring.reversestringSolution2('apple')).toBe('elppa');
  })
});

describe('test function reversestringSolution3', () => {
  test('given apple should return elppa', () => {
    expect(reversestring.reversestringSolution3('apple')).toBe('elppa');
  });
});
